"use client"

import { Button } from "@/components/ui/button";

export const SaveButton = () => {
    return (
        <Button
            variant="secondary"
            className="text-tp hover:text-ts"
        >
            Save
        </Button>
    )
}