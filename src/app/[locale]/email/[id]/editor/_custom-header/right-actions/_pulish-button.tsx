"use client"

import { Button } from "@/components/ui/button";

export const PublishButton = () => {
    return (
        <Button
            variant="secondary"
            className="text-tp hover:text-ts"
        >
            Publish
        </Button>
    )
}